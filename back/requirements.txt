# ===== requirements.txt（GPU 版，适配 RTX 3060 / CUDA 12.x）=====
# 基础 Web 框架
fastapi==0.109.0
uvicorn[standard]==0.27.0
pydantic==2.5.0

# 核心 AI 库
# torch 请单独安装 CUDA 版，例如:
#   pip install torch --index-url https://download.pytorch.org/whl/cu124
transformers>=4.40.0
accelerate>=0.30.0
sentencepiece==0.1.99

# 量化 / QLoRA（Windows 需 bitsandbytes>=0.43；当前验证可用 0.49.2）
bitsandbytes>=0.43.0
peft>=0.11.0
trl>=0.8.0

# 工具类
PyMuPDF==1.24.0
PyPDF2==3.0.1          # 新增：解决 import PyPDF2 报错
python-docx==1.2.0     # 新增：解决 import docx 报错
python-pptx==1.0.2     # 新增：解决 import pptx 报错
pandas==2.1.4          # 新增：解决 import pandas 报错
python-dotenv==1.2.2   # 新增：解决 import dotenv 报错
openpyxl==3.1.0
mysql-connector-python==9.0.0

# LangChain 全家桶（拆分独立包补充）
langchain==0.1.0
langgraph==0.0.20
langchain-openai==0.0.6 # 新增：解决 langchain_openai 导入